Player-side Map Zoom
====================

Players (and the Host's own local preview) can independently zoom
(Ctrl+wheel), pan (right/middle-drag, or scroll/Shift+scroll), and
fit-to-window. A GM setting can snap every connected view to a
configured zoom centered on the current initiative turn.
