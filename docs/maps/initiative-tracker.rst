Initiative Tracker
==================

A drag-to-reorder combat order per map (Start/Stop/Next), with
auto-skip for incapacitated participants, a facing arrow (mouse wheel to
rotate) on Character tokens, and a per-campaign choice of whether the
game clock freezes or advances a fixed amount per round while it runs.
