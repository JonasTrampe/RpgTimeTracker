Interval
========

A recurring event fired on a fixed game-time cadence (distinct from a
one-shot Alarm), useful for things like "roll a random encounter every
4 hours" - can also carry attached trigger media (see
:doc:`/media/trigger-media`).
