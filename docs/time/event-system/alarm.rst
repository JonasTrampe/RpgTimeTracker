Alarm
=====

Any number of alarms with a fixed target time, optionally recurring
(e.g. every 24 game hours). Jumping back before the target time disarms
an already-triggered alarm.

Like Timers, an alarm can carry attached trigger media (see
:doc:`/media/trigger-media`) and an advance-warning banner.
