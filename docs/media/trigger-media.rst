Trigger Media
=============

An image/video (or a sound, picked directly from disk) can be assigned
to timers, alarms, and interval events (see :doc:`/time/event-system/index`),
sent automatically on trigger, optionally fullscreen and looping instead
of auto-closing. Video additionally pauses the clock for its actual
duration, even when it only plays locally for the GM.
